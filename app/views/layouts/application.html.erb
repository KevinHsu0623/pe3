<!DOCTYPE html>
<html>
  <head>
    <title>MyApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav>
        <ul>
          <% if user_signed_in? %>
            <li><%= link_to "首頁", root_path %></li>
            <li>
              <%= link_to "登出", destroy_user_session_path, method: :delete, data: { confirm: "確定要登出嗎？" } %>
            </li>
          <% else %>
            <li><%= link_to "登入", new_user_session_path %></li>
            <li><%= link_to "註冊", new_user_registration_path %></li>
          <% end %>
        </ul>
      </nav>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer>
      <p>&copy; <%= Time.current.year %> MyApp</p>
    </footer>

    <!-- ✅ 必須載入 Rails UJS 來支援 method: :delete -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </body>
</html>
